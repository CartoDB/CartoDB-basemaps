machine:
  node:
    version: 0.10.40
dependencies:
  pre:
    - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test && sudo apt-get update && sudo apt-get install jq libstdc++6
  override:
    - npm install js-yaml vignette tilelive-http tilelive-cartodb tilejson tilelive
test:
  override:
    - ./vignette.sh
general:
  artifacts:
    - vignettes
