machine:
  node:
    version: 0.10.40
dependencies:
  override:
    - npm install js-yaml vignette tilelive-http tilelive-cartodb tilejson tilelive
test:
  override:
    - ./vignette.sh
general:
  artifacts:
    - vignettes
